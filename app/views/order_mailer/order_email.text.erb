Thank you for your order with Jungle!
=============================================

Receipt for Order #<%= @order.id %>:
<% @order_lines.each do |item, index| %>
  <%= index.to_i + 1 %>) <%= item.product.name %> x <%= item.quantity %> (<%= humanized_money_with_symbol item.product.price * item.quantity %>)
<% end %>

Total: <%= humanized_money_with_symbol @order.total_cents / 100.0 %>

  